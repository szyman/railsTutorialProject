<h1>Editing Post</h1>

<%= render 'form' %>

<!--
<%= form_for :post, url: post_path(@post.id), method: :patch do |f|%>
	<% if @post.errors.any? %>
		<h2><%= pluralize(@post.errors.count, "error")%> found</h2>
		<% @post.errors.full_messages.each do |msg|%>
			<li><%= msg %></li>
			<%end%>
		</ul>
	<%end%>

<p>
	<%= f.label :title %><br>
	<%= f.text_field :title%>
</p>

<p>
	<%= f.label :text %><br>
	<%= f.text_area :text%>
</p>

<p>
	<%= f.submit%>
</p>

<% end %>-->


<%= link_to 'Back', posts_path %>
